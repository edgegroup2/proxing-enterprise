   KKKKKK: new Date().toISOString(),cKMKK
  });KKK
});

/**
 * ===============================
 * SERVER START
 * ===============================
 */
app.listen(PORT, "0.0.0.0", () => {
  console.log(`🚀 ProxiNG Backend running on port ${PORT}`);

  // Start background jobs safely after boot
  setTimeout(() => {
    try {
      startCrons();
      console.log("⏱️ Cron jobs started");
    } catch (err) {
      console.error("❌ Failed to start crons:", err.message);
    }
  }, 3000);
});
